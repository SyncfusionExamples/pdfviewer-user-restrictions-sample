# pdfviewer-user-restrictions-sample
In the sample, we have included a user dropdown that allows switching between users. When a user is switched, annotations and form fields created by other users are restricted from being edited in the PDF Viewer.
